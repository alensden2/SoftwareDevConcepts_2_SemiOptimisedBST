public class RotateTree {
    void isRotateRequired(Node parentNode, Node currentNode, Node leftNode, Node rightNode){

    }
    Node leftRotate(Node parentNode, Node currentNode, Node leftNode){

        return currentNode;
    }
    Node rightRotate(Node parentNode, Node currentNode, Node rightNode){

        return currentNode;
    }
}
